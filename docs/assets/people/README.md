Drop person photos here (square JPG/PNG, ~400x400+), named to match the `photo:`
field in ../../data/people.yaml, e.g. erik-engberg.jpg. Empty photo = monogram avatar.
The Google Sites originals are 403 signed URLs and can't be pulled automatically.
