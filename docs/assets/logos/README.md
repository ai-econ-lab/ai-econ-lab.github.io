# Partner logos

Filed 1 September 2026. The AISCAF and WASP-HS marks belong on the Monitor: Magnus asked
Christofer (WASP-HS) on 28 August whether both could be added, and Hanna Nordin sent the
official WASP-HS files on 1 September. Consent is on record in that exchange.

| file | use | size | notes |
|---|---|---|---|
| `WASP-HS_Logotype_Blue_Full_Name.png` | **default, light backgrounds** | 1679×134, RGBA | the blue mark, transparent |
| `WASP-HS_Logotype_White_Full_Name.png` | dark backgrounds | 1679×134, palette PNG | transparent |
| `WASP-HS_Logotype_Black_Full_Name.jpg` | print, one-colour | 1679×134, RGB | **JPEG, so no transparency** — do not place it on a tinted background |
| `aiscaf_dark_ink.png` | **AISCAF default, light backgrounds** | 561×427, RGBA | transparent, ink #2e2e2e. From Julia, 31 Aug 2026 |
| `aiscaf_white_ink.png` | AISCAF on dark backgrounds | 561×427, RGBA | transparent, white ink |
| `aiscaf_highres_white_bg.jpg` | print, large sizes | 2333×1776, RGB | **white background baked in**, no transparency |
| `aiscaf_LEGACY_dark_plaque.png` | reference only | 2034×468 | the deck copy: a DARK PLAQUE, not a transparent mark. It rendered as a black box on the white Monitor page, which is what prompted asking Julia. Superseded |
| `wasphs_logo_LEGACY_deck_copy.png` | reference only | 772×118 | the older copy the presentation decks carry; superseded by the files above, kept so the two can be told apart |

**Vectors are available.** Hanna offered vectorised versions if these are too small. At 1679px
wide they are ample for the web and for a one-pager; ask her if the mark ever needs to be set
large in print.

**Aspect ratio is extreme**, 12.5:1 for WASP-HS against 1.3:1 for the new AISCAF mark (the
legacy plaque was 4.3:1, which is a different shape again, so do not reuse old sizing). The two cannot sit in
a row at the same height without the WASP-HS wordmark dominating; match them on *cap height* or
give WASP-HS its own line.

## Trim the padding, or the mark disappears

Julia's AISCAF PNGs are 561x427 **with the ink in the top 185 rows and the rest transparent**,
so 57 per cent of the canvas is empty. Setting a CSS height on the untrimmed file therefore
renders a mark barely half the size you asked for, which is exactly how it looked in the first
mock: present, but almost invisible beside WASP-HS.

**Use `aiscaf_dark_ink_trim.png` and `aiscaf_white_ink_trim.png`**, cropped to the alpha
bounding box, 561x185, ratio 3:1. The untrimmed originals are kept as delivered.

## Which AISCAF file to use

**`aiscaf_dark_ink_trim.png` on the site.** It is transparent with near-black ink, so it sits on
the Monitor's white page correctly, which the legacy plaque did not. Use `aiscaf_white_ink.png`
only on a dark ground, and the JPEG only where the background really is white and the mark is
set large. Julia sent two identical copies of the dark-ink file, `AISCAF logga.png` and
`AISCAF logga 2.png`, byte-for-byte the same; the duplicate was deleted rather than filed.

The new mark is 561×427, so it is SMALL. Ample at footer size; if it is ever set large,
the JPEG is the only high-resolution file we have and it cannot go on a tinted background.
Worth asking Julia for a vector, as Hanna offered for WASP-HS.

## Not yet placed

These are filed, not used. Where the two marks go on the Monitor is a design decision and has
not been taken: a funder strip in the footer is the obvious answer, but the Monitor's footer is
currently one line of citation text and adding two marks changes its weight.

## Standing constraint

Filing and placing the logos is not communicating. The [comms hold] stands: the Monitor and the
briefs publish, but nothing is promoted until the joint launch with the Oskar and Leo report.
Hanna has said she will be ready when that happens.
